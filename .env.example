# Variables del CLIENTE (Vite las inyecta en el bundle, son públicas).
# Copia a .env — .env está en .gitignore.
#
# La anon key es pública POR DISEÑO: lo que protege los datos es la RLS,
# no el secreto de esta clave. La service_role NUNCA va aquí.
VITE_SUPABASE_URL="https://ckjoklnmedodnrrqzqpk.supabase.co"
VITE_SUPABASE_ANON_KEY="eyJ..."
